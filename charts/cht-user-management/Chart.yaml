apiVersion: v2
name: cht-user-management
description: Chart for https://github.com/medic/cht-user-management

type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.2.0

dependencies:
  - alias: cht-user-management
    name: base-single-app
    version: "0.2.0"
    repository: https://docs.communityhealthtoolkit.org/helm-charts
    # repository: file://../base-single-app

appVersion: ""
